# Платформа для онлайн-курсов

Полнофункциональная платформа для онлайн-курсов, реализованная на стеке **Vanilla JavaScript + Node.js + MongoDB**.

## 🚀 Технологический стек

- **Frontend**: HTML5, CSS3, Vanilla JavaScript (fetch API)
- **Backend**: Node.js + Express.js
- **Database**: MongoDB + Mongoose
- **Authentication**: JWT (JSON Web Tokens)
- **Security**: bcryptjs для хеширования паролей

## 📂 Структура проекта

```
/online-course-platform
│
├── /backend
│   ├── /models          # Схемы MongoDB (Mongoose)
│   │   ├── User.js      # Модель пользователя
│   │   ├── Course.js    # Модель курса
│   │   └── Enrollment.js # Модель зачисления
│   ├── /controllers     # Логика обработки запросов
│   │   ├── authController.js
│   │   ├── courseController.js
│   │   └── statsController.js
│   ├── /routes          # Маршруты API
│   │   ├── authRoutes.js
│   │   ├── courseRoutes.js
│   │   └── statsRoutes.js
│   ├── /middleware      # Аутентификация и проверка ролей
│   │   └── auth.js
│   └── server.js        # Точка входа бэкенда
│
├── /frontend
│   ├── /css             # Стили страниц
│   │   └── style.css
│   ├── /js              # Скрипты для работы с API
│   │   ├── auth.js
│   │   ├── courses.js
│   │   ├── course-details.js
│   │   ├── my-courses.js
│   │   ├── admin.js
│   │   └── main.js
│   ├── /pages           # HTML страницы
│   │   ├── login.html
│   │   ├── register.html
│   │   ├── course-details.html
│   │   ├── my-courses.html
│   │   └── admin.html
│   └── index.html       # Главная страница
│
├── .env                 # Переменные окружения
├── package.json
└── README.md
```

## 🗄️ Архитектура данных (MongoDB)

### User Model
```javascript
{
  name: String (required),
  email: String (required, unique),
  password: String (required, hashed),
  role: String (enum: 'student' | 'instructor', default: 'student')
}
```

### Course Model (Reference + Embedded)
```javascript
{
  title: String (required),
  description: String (required),
  price: Number (required),
  instructor: ObjectId (Reference to User),
  lessons: [ // Embedded документы
    {
      title: String,
      content: String,
      duration: Number
    }
  ]
}
```
- **instructor**: Reference (связь по ID)
- **lessons**: Embedded (массив объектов внутри документа)
- **Индекс**: Составной индекс на `title` и `instructor` для оптимизации поиска

### Enrollment Model (Reference)
```javascript
{
  student_id: ObjectId (Reference to User),
  course_id: ObjectId (Reference to Course),
  progress: Number (0-100, default: 0),
  enrolledAt: Date
}
```
- **Уникальный индекс**: `{ student_id: 1, course_id: 1 }` - один студент может быть зачислен на курс только один раз

## 🔌 API Endpoints

### Аутентификация (`/api/auth`)

| Метод | Endpoint | Описание | Доступ |
|-------|----------|----------|--------|
| POST | `/api/auth/register` | Регистрация пользователя | Публичный |
| POST | `/api/auth/login` | Вход в систему | Публичный |
| GET | `/api/auth/me` | Получить текущего пользователя | Аутентифицированный |

### Курсы (`/api/courses`)

| Метод | Endpoint | Описание | Доступ |
|-------|----------|----------|--------|
| GET | `/api/courses` | Получить все курсы | Публичный |
| GET | `/api/courses/:id` | Получить курс по ID | Публичный |
| POST | `/api/courses` | Создать курс | Инструктор |
| PATCH | `/api/courses/:id` | Обновить курс | Инструктор (владелец) |
| DELETE | `/api/courses/:id` | Удалить курс | Инструктор (владелец) |
| POST | `/api/courses/:id/lessons` | Добавить урок (использует `$push`) | Инструктор (владелец) |
| POST | `/api/courses/:id/enroll` | Записаться на курс | Студент |
| PATCH | `/api/courses/:id/progress` | Обновить прогресс (использует `$inc`) | Студент |
| GET | `/api/courses/student/my-courses` | Получить курсы студента | Студент |

### Статистика (`/api/stats`)

| Метод | Endpoint | Описание | Доступ |
|-------|----------|----------|--------|
| GET | `/api/stats` | Статистика по курсам (агрегация) | Инструктор |

**Агрегация включает:**
- Количество студентов на каждом курсе
- Общая выручка по каждому курсу
- Общая статистика (всего студентов, выручка, курсов)

## 🛠️ Установка и запуск

### Требования
- Node.js (v14 или выше)
- MongoDB (локально или MongoDB Atlas)

### Шаги установки

1. **Клонировать репозиторий** (или использовать текущую папку)

2. **Установить зависимости:**
```bash
npm install
```

3. **Настроить переменные окружения:**
Создайте файл `.env` в корне проекта:
```env
MONGODB_URI=mongodb://localhost:27017/online-courses
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
PORT=3000
```

4. **Запустить MongoDB:**
Убедитесь, что MongoDB запущен локально, или используйте MongoDB Atlas и укажите URI в `.env`.

5. **Заполнить базу данных начальными данными (рекомендуется):**
Для того чтобы платформа работала как настоящая LMS с курсами и данными, запустите скрипт заполнения базы:
```bash
npm run seed
```
Этот скрипт создаст:
- 3 инструктора с готовыми курсами
- 2 студента с примерами зачислений
- 6 курсов с уроками (всего более 30 уроков)
- Примеры зачислений студентов на курсы

**Учетные данные для входа после заполнения:**
- Инструкторы: `alex.petrov@lms.com` / `instructor123`
- Студенты: `ivan.student@lms.com` / `student123`

6. **Запустить сервер:**
```bash
npm start
```
или для разработки с автоматической перезагрузкой:
```bash
npm run dev
```

7. **Открыть в браузере:**
```
http://localhost:3000
```

## ✨ Основные функции

### Для студентов:
- ✅ Регистрация и вход
- ✅ Просмотр всех доступных курсов
- ✅ Запись на курс
- ✅ Просмотр своих курсов
- ✅ Отслеживание прогресса обучения
- ✅ Обновление прогресса (демонстрация `$inc`)

### Для инструкторов:
- ✅ Регистрация и вход
- ✅ Создание курсов
- ✅ Добавление уроков к курсам (демонстрация `$push`)
- ✅ Редактирование и удаление своих курсов
- ✅ Просмотр статистики (агрегация MongoDB)
- ✅ Динамическое удаление курсов из DOM без перезагрузки

## 🔒 Безопасность

- **Хеширование паролей**: Используется bcryptjs
- **JWT токены**: Для аутентификации и авторизации
- **Валидация данных**: Проверка на бэкенде перед сохранением
- **Проверка прав доступа**: Middleware для проверки ролей и владельцев

## 📊 Оптимизация

- **Индексы MongoDB**: 
  - Составной индекс на `Course.title` и `Course.instructor`
  - Уникальный индекс на `Enrollment.student_id` и `Enrollment.course_id`
- **Оптимизация запросов**: Использование `populate()` для связей

## 🎯 Демонстрация возможностей MongoDB

1. **Embedded документы**: Уроки (`lessons`) встроены в документ курса
2. **Reference документы**: Связи `instructor`, `student_id`, `course_id` через ObjectId
3. **Операторы MongoDB**:
   - `$push` - добавление урока в массив
   - `$inc` - инкремент прогресса
4. **Агрегация**: Сложные запросы для статистики с использованием `$lookup`, `$group`, `$project`

## 📝 Примеры использования API

### Регистрация студента:
```javascript
POST /api/auth/register
{
  "name": "Иван Иванов",
  "email": "ivan@example.com",
  "password": "password123",
  "role": "student"
}
```

### Создание курса (требует токен инструктора):
```javascript
POST /api/courses
Headers: { "Authorization": "Bearer <token>" }
{
  "title": "JavaScript для начинающих",
  "description": "Изучите основы JavaScript",
  "price": 2999
}
```

### Добавление урока:
```javascript
POST /api/courses/:id/lessons
Headers: { "Authorization": "Bearer <token>" }
{
  "title": "Введение в переменные",
  "content": "Переменные используются для хранения данных...",
  "duration": 30
}
```

### Обновление прогресса:
```javascript
PATCH /api/courses/:id/progress
Headers: { "Authorization": "Bearer <token>" }
{
  "increment": 10
}
```

## 🌱 Заполнение базы данных

Для имитации настоящей LMS системы с курсами и данными используйте скрипт заполнения:

```bash
npm run seed
```

**Что создается:**
- ✅ 3 инструктора с готовыми аккаунтами
- ✅ 2 студента с примерами зачислений
- ✅ 6 полноценных курсов с уроками:
  - JavaScript для начинающих (5 уроков)
  - Python: от основ к продвинутому уровню (6 уроков)
  - Веб-дизайн и UI/UX (5 уроков)
  - React: современная разработка (6 уроков)
  - Базы данных и SQL (5 уроков)
  - Node.js и серверная разработка (6 уроков)
- ✅ Примеры зачислений студентов на курсы с прогрессом

**Учетные данные:**
- Инструкторы: `alex.petrov@lms.com`, `maria.ivanova@lms.com`, `dmitry.sidorov@lms.com` (пароль: `instructor123`)
- Студенты: `ivan.student@lms.com`, `elena.student@lms.com` (пароль: `student123`)

**Очистка базы данных:**
```bash
npm run clear-db
```

**Проверка наличия курсов:**
```bash
npm run check-courses
```
Эта команда покажет количество курсов, пользователей и зачислений в базе данных.

## 🐛 Решение проблем

**Курсы не отображаются после регистрации:**
- Запустите скрипт заполнения базы: `npm run seed`
- Это создаст начальные данные (курсы, инструкторов, студентов)

**Ошибка подключения к MongoDB:**
- Убедитесь, что MongoDB запущен
- Проверьте URI в `.env`

**Ошибка CORS:**
- CORS настроен для всех источников (в продакшене ограничьте)

**Токен не работает:**
- Проверьте, что токен сохраняется в localStorage
- Убедитесь, что JWT_SECRET установлен в `.env`

## 📄 Лицензия

ISC

## 👨‍💻 Авторы

Проект создан в рамках учебного задания по NoSQL базам данным.

### Вклад участников проекта:

**Студент 1:**
- Разработка backend архитектуры (Express.js, MongoDB, Mongoose)
- Реализация REST API endpoints (13 endpoints)
- Создание моделей данных (User, Course, Enrollment)
- Реализация аутентификации и авторизации (JWT, bcrypt)
- Разработка агрегационных запросов для статистики
- Оптимизация запросов с использованием индексов
- Реализация advanced операций ($push, $inc, deleteMany)
- Написание документации (README.md, API documentation)

**Студент 2:**
- Разработка frontend интерфейса (Vanilla JavaScript, HTML5, CSS3)
- Создание всех 6 страниц приложения
- Реализация взаимодействия с REST API через Fetch API
- Разработка современного футуристичного дизайна
- Реализация CRUD операций на фронтенде
- Создание скриптов для заполнения и проверки базы данных
- Тестирование функционала и исправление ошибок
- Улучшение пользовательского опыта (UX)

---

**Примечание**: В продакшене обязательно:
- Используйте более сложный JWT_SECRET
- Ограничьте CORS
- Добавьте rate limiting
- Используйте HTTPS
- Настройте логирование ошибок

