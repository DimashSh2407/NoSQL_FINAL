# Как проверить наличие курсов

## 🔍 Способы проверки курсов

### 1. Через веб-интерфейс (самый простой способ)

1. Запустите сервер:
   ```bash
   npm start
   ```

2. Откройте в браузере:
   ```
   http://localhost:3000
   ```

3. На главной странице в разделе **"Доступные курсы"** должны отображаться карточки курсов.

4. Если видите сообщение **"Курсы пока не добавлены"** — база данных пуста, нужно запустить скрипт заполнения.

---

### 2. Через API напрямую (в браузере)

1. Убедитесь, что сервер запущен (`npm start`)

2. Откройте в браузере:
   ```
   http://localhost:3000/api/courses
   ```

3. Вы должны увидеть JSON-ответ со списком всех курсов:
   ```json
   [
     {
       "_id": "...",
       "title": "JavaScript для начинающих",
       "description": "...",
       "price": 2999,
       "instructor": {
         "_id": "...",
         "name": "Александр Петров",
         "email": "alex.petrov@lms.com"
       },
       "lessons": [...],
       "createdAt": "...",
       "updatedAt": "..."
     },
     ...
   ]
   ```

4. Если видите пустой массив `[]` — курсов нет в базе.

---

### 3. Через терминал (curl или PowerShell)

**Windows PowerShell:**
```powershell
Invoke-RestMethod -Uri "http://localhost:3000/api/courses" -Method Get | ConvertTo-Json
```

**Или через curl (если установлен):**
```bash
curl http://localhost:3000/api/courses
```

**Или через браузер:**
Просто откройте `http://localhost:3000/api/courses` в адресной строке.

---

### 4. Через консоль браузера (Developer Tools)

1. Откройте главную страницу: `http://localhost:3000`

2. Нажмите `F12` (или `Ctrl+Shift+I`) чтобы открыть Developer Tools

3. Перейдите на вкладку **Console**

4. Выполните команду:
   ```javascript
   fetch('http://localhost:3000/api/courses')
     .then(res => res.json())
     .then(courses => {
       console.log('Количество курсов:', courses.length);
       console.log('Курсы:', courses);
     });
   ```

5. В консоли увидите количество и список всех курсов.

---

### 5. Напрямую в MongoDB

Если у вас установлен MongoDB Compass или доступ к MongoDB через терминал:

**Через MongoDB Compass:**
1. Подключитесь к базе: `mongodb://localhost:27017/online-courses`
2. Откройте коллекцию `courses`
3. Проверьте количество документов

**Через MongoDB Shell (mongosh):**
```javascript
// Подключитесь к базе
use online-courses

// Посчитайте количество курсов
db.courses.countDocuments()

// Показать все курсы
db.courses.find().pretty()

// Показать только названия курсов
db.courses.find({}, {title: 1, instructor: 1, price: 1})
```

---

## 🚀 Быстрая проверка (пошагово)

### Шаг 1: Проверьте, запущен ли сервер
```bash
# В терминале должно быть видно:
# 🚀 Сервер запущен на порту 3000
# ✅ Подключено к MongoDB
```

### Шаг 2: Проверьте API
Откройте в браузере: `http://localhost:3000/api/courses`

**Если видите JSON с курсами** ✅ — всё работает!

**Если видите пустой массив `[]`** ❌ — нужно заполнить базу:
```bash
npm run seed
```

### Шаг 3: Проверьте веб-интерфейс
Откройте: `http://localhost:3000`

На главной странице должны быть карточки курсов.

---

## 🔧 Решение проблем

### Проблема: "Курсы пока не добавлены"

**Решение:**
1. Запустите скрипт заполнения базы:
   ```bash
   npm run seed
   ```

2. Дождитесь завершения (должно появиться сообщение "🎉 База данных успешно заполнена!")

3. Обновите страницу в браузере (`F5`)

### Проблема: Ошибка подключения к API

**Проверьте:**
1. Сервер запущен? (`npm start`)
2. MongoDB запущен?
3. Правильный ли порт? (по умолчанию 3000)
4. Проверьте консоль сервера на наличие ошибок

### Проблема: API возвращает ошибку

**Проверьте:**
1. Откройте консоль браузера (`F12` → Console)
2. Посмотрите на ошибки в консоли
3. Проверьте Network вкладку в Developer Tools

---

## 📊 Что должно быть после `npm run seed`

После успешного выполнения скрипта заполнения:

- ✅ **6 курсов** с полным описанием
- ✅ **Более 30 уроков** в общей сложности
- ✅ **3 инструктора** с готовыми аккаунтами
- ✅ **2 студента** с примерами зачислений
- ✅ **3 зачисления** студентов на курсы

**Проверка:**
```bash
# В браузере откройте:
http://localhost:3000/api/courses

# Должно вернуться 6 курсов в JSON формате
```

